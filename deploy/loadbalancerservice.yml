apiVersion: v1
kind: Service
metadata:  
  name: whale-svc
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-internal: 0.0.0.0/0
spec:
  selector:    
    app: whale-app
  type: LoadBalancer
  ports:  
  - name: http
    port: 80
    targetPort: 5000
    nodePort: 30036
    protocol: TCP